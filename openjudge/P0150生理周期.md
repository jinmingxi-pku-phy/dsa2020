P0150生理周期+2400011574

AC答案1：暴力法

```
s=list(map(int,input().split()))
p=s[0]%23
e=s[1]%28
i=s[2]%33
d=s[3]
for n in range(d+1,d+21253):
    if (n-p)%23 == 0 and (n-e)%28 == 0 and (n-i)%33 ==0:
        a=n-d
        break
print(a)
```

总结教训：刚开始的时候RE了，主要是要注意范围到d+21252而不仅是21252。

AC答案2：中国剩余定理（代码有参考其他工具与题解）

引：中国剩余定理：解决同余问题，即给定模数下找到一个满足多个同余方程的解。

```
import math
def lcm(a, b):
    return a * b // math.gcd(a, b)
def mod_inverse(a, m):
    m0, x0, x1 = m, 0, 1
    if m == 1:
        return 0
    while a > 1:
        q = a // m
        m, a = a % m, m
        x0, x1 = x1 - q * x0, x0
    if x1 < 0:
        x1 += m0
    return x1
def find_next_triple_peak(p, e, i):
    m1, m2, m3 = 23, 28, 33
    M = lcm(lcm(m1, m2), m3)
    M1, M2, M3 = M // m1, M // m2, M // m3
    y1, y2, y3 = mod_inverse(M1, m1), mod_inverse(M2, m2), mod_inverse(M3, m3)

    n = (p * M1 * y1 + e * M2 * y2 + i * M3 * y3) % M
    return n, M
p, e, i, d = map(int, input().split())
n, M = find_next_triple_peak(p, e, i)
if n <= d:
        n += M
print(n-d)
```

总结：再做完暴力法之后查到了一个看起来复杂实则很高级的做法，分享一下。



运行![屏幕截图 2025-02-19 145600](C:\Users\qihon\Pictures\Screenshots\屏幕截图 2025-02-19 145600.png)截图：

![屏幕截图 2025-02-19 151135](C:\Users\qihon\Pictures\Screenshots\屏幕截图 2025-02-19 151135.png)